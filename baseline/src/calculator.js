function Calculator() {
}

Calculator.prototype.add = function (aNumber, bNumber) {
    return aNumber + bNumber;
};

Calculator.prototype.subtract = function (aNumber, bNumber) {
    return aNumber - bNumber;
};

Calculator.prototype.multiply = function (aNumber, bNumber) {
    return aNumber * bNumber;
};